-- ============================================================
-- Результат выполнения задачи (обязателен для статуса «Готово»)
-- Выполнить в Supabase → SQL Editor → New query → Run
-- ============================================================

alter table public.tasks add column if not exists result text;
