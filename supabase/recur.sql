-- ============================================================
-- Повторяющиеся задачи (ежедневные/еженедельные и т.д.)
-- Выполнить в Supabase → SQL Editor → New query → Run
-- ============================================================

alter table public.tasks add column if not exists recur text;
